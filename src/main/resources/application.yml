server:
  port: 8080

spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

logging:
  level:
#    org.hibernate.SQL: debug
    org.hibernate.type: trace